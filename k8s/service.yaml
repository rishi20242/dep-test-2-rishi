apiVersion: v1
kind: Service
metadata:
  name: sample-service
spec:
  type: LoadBalancer        # Changed from NodePort
  selector:
    app: sample-app
  ports:
  - port: 80                # The port users will hit
    targetPort: 5000        # The port your container listens on
